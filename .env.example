# TWODRAPES optional local configuration.
# The app works without this file. Defaults are shown below.

PORT=8080
FACTORY_PORT=8081
AMAZON_PORT=8082
HOST=0.0.0.0

# Auth is enabled by default.
# Set NO_AUTH=1 to disable authentication (local convenience only).
# NO_AUTH=1
ADMIN_USER=admin
ADMIN_PASSWORD=twodrapes2025

# Sales calculation defaults.
PAYPAL_FEE_RATE=0.044
USD_RMB_RATE=6.8
